// Arrow fuctions
function somar(a,b) {
  return a + b
}

(a, b) => a+b

// Tempalte strings 
const a = 'Primeiro' 
const b =  'segundo' 
const  concatenado01 = a + '' + b

const concatenado02 = `${a} ${b}`

// Exportação e importação
export const nome = 'fabio'
